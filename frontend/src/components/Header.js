import Row from "react-bootstrap/Row"

const Header = (props) => {

  return (<Row>
    <div className="w-50 mx-auto">
      <img alt="title" src="/ServerlessStockWatch.png"/>
    </div>
  </Row>);
}

export default Header;
